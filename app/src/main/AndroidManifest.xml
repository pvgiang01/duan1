<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project1group3" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project1Group3" >
        <activity android:name=".LoginSVActivity" >
        </activity>
        <activity android:name=".LoginGvActivity" />
        <activity android:name=".LoginAdminActivity" />
        <activity android:name=".QuanTriMainActivity" >
        </activity>
        <activity
            android:name=".SplashScreenActivity
            android:theme="@style/Theme.SplashTheme" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>